{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Git Add Commit Push",
      "type": "shell",
      "command": "${workspaceFolder}/gitpush.sh",
      "args": [
        "${relativeFile}"
      ]
    }
  ]
}